[package]
name = "moss-core"
version.workspace = true
edition.workspace = true

[lib]
name = "moss"
path = "src/lib.rs"

[dependencies]
libc = "0.2"
moss-sys = { path = "../moss-sys" }
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
