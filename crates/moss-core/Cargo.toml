[package]
name = "moss-core"
version = "0.1.0"
edition = "2024"

[lib]
name = "moss"
path = "src/lib.rs"

[dependencies]
libc = "0.2"
moss-sys = { path = "../moss-sys" }
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
